import { describe, it } from "vitest";

// Not: Hook testleri için renderHook (RTL) veya benzeri yardımcılar eklenene kadar
// bu dosya iskelet olarak bırakıldı. Gerçek testler mock servislerle eklenecek.

describe("useCashDashboardData", () => {
  it.skip("kapanan oturumları sıralamalı ve kasa özetini oluşturmalı", () => {
    // arrange: tarih aralığı ve sahte oturum/işlem verileri
    // act: hook'u çağır
    // assert: closedSessions, lastClosedSession, cashData.dailyData
  });
});


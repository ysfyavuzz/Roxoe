import { describe, it } from 'vitest'

describe('[integration] POS sales flow', () => {
  it.skip('cart -> payment -> receipt -> stock update (to be implemented with DB/electron mocks)', () => {})
})

